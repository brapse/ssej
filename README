SSEJ
====

Pronouced "essay"

# Maps
$ ssej 'line.length' some_file haystack.log
$ ssej 'Object.keys(JSON.parse(line))' hatstack.log

# Parsers
$ ssej --parser='JSON.parse' 'Object.keys(line)' haystack.log

# Filters
$ ssej --filter='line.indexOf('needle')' 'line.length' haystack.log
$ ssej --filter='line.json.event=="CLICK"'

# Reduce
$ ssej --count='json.event_type'
$ ssej --count='[json.event_type, json.referer]' logFiles

# Reduce with labels
$ssej --count=['json.event_type, {abbrev: json.refer.slice(0, 10)}]' myFiles
